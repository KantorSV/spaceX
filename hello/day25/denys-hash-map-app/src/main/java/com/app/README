1) a.equals(b) --> b.equals(a)
симметричность

2) a.equals(a) --> true
рефлексивность

3) a.equals(b) ; b.equals(c)--> a.equals(c)
транзитивность


4) если у 2 объекта еквивалентны
(a.equals(b))  -->

их хеш коды равны
a.hashCode()==b.hashCode();

Обратное не верно:
из равенства хеш кодов не слежует еквивалентность 2 обьектов
a.hashCode()==b.hashCode() --> не слежует a.equals(b)
